<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>랭킹 - 스터디 플래너</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f5f7fa;
            padding: 24px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        .page-header {
            background: white;
            padding: 24px;
            border-radius: 8px;
            margin-bottom: 24px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            border: 1px solid #e8e8e8;
        }

        .page-header h1 {
            color: #262626;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .page-header p {
            color: #8c8c8c;
            font-size: 14px;
        }

        .tabs {
            background: white;
            padding: 8px;
            border-radius: 8px;
            margin-bottom: 24px;
            border: 1px solid #e8e8e8;
            display: flex;
            gap: 8px;
        }

        .tab {
            flex: 1;
            padding: 12px;
            border: none;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            color: #595959;
            border-radius: 4px;
            background: transparent;
            transition: 0.3s;
        }

        .tab.active {
            background: #1890ff;
            color: white;
        }

        .tab:hover {
            background: #fafafa;
        }

        .season-info {
            background: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 24px;
            text-align: center;
            border: 1px solid #e8e8e8;
        }

        .season-info h2 {
            color: #1890ff;
            font-size: 18px;
            margin-bottom: 6px;
        }

        .season-info p {
            font-size: 13px;
            color: #8c8c8c;
        }

        .ranking-list {
            list-style: none;
        }

        .ranking-item {
            background: #fafafa;
            padding: 16px;
            margin-bottom: 12px;
            border-radius: 4px;
            display: flex;
            align-items: center;
            gap: 16px;
            border: 1px solid #f0f0f0;
            transition: 0.3s;
        }

        .ranking-item:hover {
            background: white;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            transform: translateX(2px);
        }

        .ranking-item.my-rank {
            background: #e6f7ff;
            border-color: #91d5ff;
        }

        .user-info {
            flex: 1;
        }

        .username {
            font-weight: 600;
            font-size: 16px;
        }

        .user-diploma {
            font-size: 13px;
            color: #8c8c8c;
        }

        .score {
            font-size: 20px;
            font-weight: 700;
            color: #1890ff;
        }

        .pvp-btn {
            padding: 8px 16px;
            background: #1890ff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 13px;
            transition: 0.3s;
        }

        .pvp-btn:hover {
            background: #40a9ff;
        }

        .back-link {
            display: inline-block;
            margin-top: 24px;
            padding: 10px 20px;
            background: white;
            border: 1px solid #d9d9d9;
            border-radius: 4px;
            color: #1890ff;
            text-decoration: none;
        }

        .back-link:hover {
            background: #e6f7ff;
        }
    </style>
</head>

<body>
<div class="container">

    <div class="page-header">
        <h1>🏆 랭킹</h1>
        <p>학습 성과를 비교하고 경쟁하세요</p>
    </div>

    <!-- 주간 랭킹 제거 -->
    <div class="tabs">
        <button class="tab active" onclick="switchTab('season')">시즌 랭킹</button>
        <button class="tab" onclick="switchTab('daily')">일일 랭킹</button>
    </div>

    <div class="season-info">
        <% if (currentSeason) { %>
            <h2>🎯 <%= currentSeason.name %></h2>
            <p><%= currentSeason.start_date %> - <%= currentSeason.end_date %></p>
            <% if (currentSeason.description) { %>
                <p style="margin-top:6px; color:#595959;"><%= currentSeason.description %></p>
            <% } %>
        <% } else { %>
            <h2>🎯 시즌 정보 없음</h2>
            <p>현재 진행 중인 시즌이 없습니다</p>
        <% } %>
    </div>

    <ul class="ranking-list">
        <% rankings.forEach((user, index) => { %>
            <li class="ranking-item <%= user.id === session.user.id ? 'my-rank' : '' %>">
                <div>
                    <%= index === 0 ? '🥇' : index === 1 ? '🥈' : index === 2 ? '🥉' : index + 1 %>
                </div>

                <div class="user-info">
                    <div class="username">
                        <%= user.username %>
                        <%= user.id === session.user.id ? '(나)' : '' %>
                    </div>
                    <div class="user-diploma"><%= user.diploma %></div>
                </div>

                <div class="score"><%= user.total_score %>점</div>

                <% if (user.id !== session.user.id) { %>
                    <button class="pvp-btn"
                            onclick="challengePVP('<%= user.id %>', '<%= user.username %>')">
                        ⚔️ 도전
                    </button>
                <% } %>
            </li>
        <% }) %>
    </ul>

    <a href="/home" class="back-link">← 홈으로 돌아가기</a>
</div>

<script>
    function switchTab(type) {
        document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
        event.target.classList.add('active');
        console.log('Switch to', type);
    }

    function challengePVP(userId, username) {
        if (confirm(`${username}님에게 PVP를 신청하시겠습니까?`)) {
            location.href = `/pvp/challenge/${userId}`;
        }
    }
</script>

</body>
</html>
