<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>문제 풀이 - 스터디 플래너</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        h1 {
            color: #667eea;
            margin-bottom: 30px;
            font-size: 32px;
            text-align: center;
        }

        .problem-filters {
            background: #f5f7ff;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .filter-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .filter-group label {
            font-weight: 600;
            color: #667eea;
            font-size: 14px;
        }

        .filter-group select {
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            transition: border-color 0.3s;
        }

        .filter-group select:focus {
            outline: none;
            border-color: #667eea;
        }

        .problem-list {
            list-style: none;
        }

        .problem-item {
            background: #f9f9f9;
            padding: 25px;
            margin-bottom: 15px;
            border-radius: 12px;
            border: 2px solid #e0e0e0;
            transition: all 0.3s;
            cursor: pointer;
        }

        .problem-item:hover {
            border-color: #667eea;
            transform: translateX(5px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.2);
        }

        .problem-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .problem-number {
            font-size: 18px;
            font-weight: 700;
            color: #667eea;
        }

        .problem-score {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
        }

        .problem-info {
            display: flex;
            gap: 20px;
            margin-bottom: 10px;
        }

        .info-badge {
            background: white;
            padding: 5px 12px;
            border-radius: 6px;
            font-size: 13px;
            color: #666;
            border: 1px solid #ddd;
        }

        .problem-title {
            font-size: 16px;
            color: #333;
            font-weight: 500;
            line-height: 1.5;
        }

        .solve-btn {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 10px;
            transition: transform 0.2s;
        }

        .solve-btn:hover {
            transform: translateY(-2px);
        }

        .stats {
            display: flex;
            gap: 20px;
            background: #f5f7ff;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
        }

        .stat-item {
            flex: 1;
            text-align: center;
        }

        .stat-value {
            font-size: 28px;
            font-weight: 700;
            color: #667eea;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            color: #666;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #999;
        }

        .empty-state-icon {
            font-size: 64px;
            margin-bottom: 20px;
        }

        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #764ba2;
        }

        .problem-solved {
            opacity: 0.6;
            position: relative;
        }

        .solved-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            background: #4caf50;
            color: white;
            padding: 5px 12px;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📝 문제 풀이</h1>

        <div class="stats">
            <div class="stat-item">
                <div class="stat-value"><%= stats.totalSolved %></div>
                <div class="stat-label">푼 문제</div>
            </div>
            <div class="stat-item">
                <div class="stat-value"><%= stats.totalScore %></div>
                <div class="stat-label">총 점수</div>
            </div>
            <div class="stat-item">
                <div class="stat-value"><%= stats.accuracy %>%</div>
                <div class="stat-label">정확도</div>
            </div>
        </div>

        <div class="problem-filters">
            <div class="filter-group">
                <label for="subject">과목</label>
                <select id="subject" onchange="filterProblems()">
                    <option value="">전체</option>
                    <option value="국어">국어</option>
                    <option value="수학">수학</option>
                    <option value="영어">영어</option>
                    <option value="사회">사회</option>
                    <option value="과학">통합과학</option>
                    <option value="역사">역사</option>
                </select>
            </div>
            <div class="filter-group">
                <label for="difficulty">난이도</label>
                <select id="difficulty" onchange="filterProblems()">
                    <option value="">전체</option>
                    <option value="쉬움">쉬움</option>
                    <option value="보통">보통</option>
                    <option value="어려움">어려움</option>
                </select>
            </div>
            <div class="filter-group">
                <label for="type">유형</label>
                <select id="type" onchange="filterProblems()">
                    <option value="">전체</option>
                    <option value="모의고사">모의고사</option>
                    <option value="문제집">문제집</option>
                </select>
            </div>
        </div>

        <% if (problems.length > 0) { %>
            <ul class="problem-list">
                <% problems.forEach(problem => { %>
                    <li class="problem-item <%= problem.solved ? 'problem-solved' : '' %>" 
                        onclick="location.href='/problem/<%= problem.id %>'">
                        <% if (problem.solved) { %>
                            <div class="solved-badge">✓ 완료</div>
                        <% } %>
                        <div class="problem-header">
                            <div class="problem-number">문제 #<%= problem.number %></div>
                            <div class="problem-score"><%= problem.score %>점</div>
                        </div>
                        <div class="problem-info">
                            <span class="info-badge">📚 <%= problem.subject %></span>
                            <span class="info-badge">⭐ <%= problem.difficulty %></span>
                            <span class="info-badge">📖 <%= problem.type %></span>
                        </div>
                        <div class="problem-title"><%= problem.title %></div>
                        <button class="solve-btn" onclick="event.stopPropagation(); location.href='/problem/<%= problem.id %>/solve'">
                            <%= problem.solved ? '다시 풀기' : '풀이 시작' %>
                        </button>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <div class="empty-state">
                <div class="empty-state-icon">📚</div>
                <p>아직 등록된 문제가 없습니다.</p>
                <p>관리자가 문제를 추가할 때까지 기다려주세요.</p>
            </div>
        <% } %>

        <a href="/home" class="back-link">← 홈으로 돌아가기</a>
    </div>

    <script>
        function filterProblems() {
            const subject = document.getElementById('subject').value;
            const difficulty = document.getElementById('difficulty').value;
            const type = document.getElementById('type').value;
            
            const params = new URLSearchParams();
            if (subject) params.append('subject', subject);
            if (difficulty) params.append('difficulty', difficulty);
            if (type) params.append('type', type);
            
            location.href = '/problem?' + params.toString();
        }
    </script>
</body>
</html>