<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>홈 - 스터디 플래너</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f5f7fa;
            padding: 24px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .page-header {
            background: white;
            padding: 24px;
            border-radius: 8px;
            margin-bottom: 24px;
            border: 1px solid #e8e8e8;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 16px;
        }

        .page-header h1 {
            font-size: 24px;
            font-weight: 600;
            color: #262626;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .user-info {
            color: #595959;
            font-size: 14px;
            font-weight: 500;
        }

        .header-btn {
            padding: 10px 18px;
            border-radius: 4px;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.3s;
            display: inline-block;
            cursor: pointer;
        }

        .header-btn.primary {
            background: #1890ff;
            color: white;
            border: none;
        }

        .header-btn.primary:hover {
            background: #40a9ff;
        }

        .header-btn.secondary {
            background: white;
            color: #595959;
            border: 1px solid #d9d9d9;
        }

        .header-btn.secondary:hover {
            background: #fafafa;
            border-color: #40a9ff;
            color: #1890ff;
        }

        .nav-menu {
            background: white;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 24px;
            border: 1px solid #e8e8e8;
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .nav-menu a {
            padding: 10px 18px;
            border-radius: 4px;
            text-decoration: none;
            font-size: 14px;
            color: #595959;
            font-weight: 500;
            transition: all 0.3s;
        }

        .nav-menu a.active,
        .nav-menu a:hover {
            background: #e6f7ff;
            color: #1890ff;
        }

        .dashboard {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 24px;
        }

        .card {
            background: white;
            padding: 24px;
            border-radius: 8px;
            border: 1px solid #e8e8e8;
        }

        .card h2 {
            font-size: 18px;
            font-weight: 600;
            color: #262626;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .stat-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .stat-item:last-child {
            border-bottom: none;
        }

        .stat-label {
            color: #8c8c8c;
            font-size: 14px;
        }

        .stat-value {
            font-weight: 600;
            color: #262626;
            font-size: 15px;
        }

        .d-day-card {
            text-align: center;
        }

        .d-day {
            font-size: 48px;
            font-weight: 700;
            color: #1890ff;
            margin: 20px 0;
        }

        .todo-preview {
            list-style: none;
        }

        .todo-preview li {
            padding: 12px;
            border-left: 3px solid #1890ff;
            margin-bottom: 8px;
            background: #fafafa;
            border-radius: 4px;
            font-size: 14px;
            color: #595959;
        }

        .ranking-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px;
            border-bottom: 1px solid #f0f0f0;
            background: #fafafa;
            margin-bottom: 8px;
            border-radius: 4px;
        }

        .ranking-item:last-child {
            margin-bottom: 0;
        }

        .rank-info {
            display: flex;
            align-items: center;
            gap: 12px;
            flex: 1;
        }

        .rank {
            font-weight: 700;
            color: #1890ff;
            font-size: 16px;
            min-width: 30px;
        }

        .rank.gold { color: #faad14; }
        .rank.silver { color: #8c8c8c; }
        .rank.bronze { color: #d4380d; }

        .username {
            color: #262626;
            font-weight: 500;
            font-size: 14px;
        }

        .score {
            font-weight: 600;
            color: #1890ff;
            font-size: 15px;
        }

        .empty-state {
            text-align: center;
            color: #8c8c8c;
            padding: 20px;
            font-size: 14px;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background: #f0f0f0;
            border-radius: 4px;
            overflow: hidden;
            margin-top: 12px;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #1890ff 0%, #40a9ff 100%);
            transition: width 0.5s ease;
        }

        @media (max-width: 768px) {
            body {
                padding: 16px;
            }

            .dashboard {
                grid-template-columns: 1fr;
            }

            .page-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .header-actions {
                width: 100%;
                flex-wrap: wrap;
            }

            .d-day {
                font-size: 36px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="page-header">
            <h1>📚 스터디 플래너</h1>
            <div class="header-actions">
                <span class="user-info"><%= user.student_id %>번 <%= user.username %>님</span>
                <form action="/auth/logout" method="POST" style="display: inline;">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="header-btn secondary">🚪 로그아웃</button>
                </form>
            </div>
        </div>

        <div class="nav-menu">
            <a href="/home" class="active">홈</a>
            <a href="/todo">할 일</a>
            <a href="/calendar">달력</a>
            <a href="/ranking">랭킹</a>
            <a href="/problem">문제 풀이</a>
            <a href="/pvp">PVP</a>
            <a href="/mypage">마이페이지</a>
        </div>

        <div class="dashboard">
            <div class="card d-day-card">
                <h2>⏰ 시즌 종료까지</h2>
                <div class="d-day">D-<%= dday || 0 %></div>
                <div class="stat-item">
                    <span class="stat-label">시즌 시작일</span>
                    <span class="stat-value"><%= season?.start_date || '-' %></span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">시즌 종료일</span>
                    <span class="stat-value"><%= season?.end_date || '-' %></span>
                </div>
            </div>

            <div class="card">
                <h2>📊 남은 공부량</h2>
                <div class="stat-item">
                    <span class="stat-label">오늘 할 일</span>
                    <span class="stat-value"><%= todos?.total || 0 %>개</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">완료</span>
                    <span class="stat-value" style="color: #52c41a;"><%= todos?.completed || 0 %>개</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label">미완료</span>
                    <span class="stat-value" style="color: #ff4d4f;"><%= (todos?.total || 0) - (todos?.completed || 0) %>개</span>
                </div>
                <% 
                const totalTodos = todos?.total || 0;
                const completedTodos = todos?.completed || 0;
                const progressPercent = totalTodos > 0 ? Math.round((completedTodos / totalTodos) * 100) : 0;
                %>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: <%= progressPercent %>%;"></div>
                </div>
            </div>

            <div class="card">
                <h2>🏆 시즌 랭킹</h2>
                <% if (seasonRanking && seasonRanking.length > 0) { %>
                    <% seasonRanking.slice(0, 3).forEach((rank, index) => { %>
                        <div class="ranking-item">
                            <div class="rank-info">
                                <span class="rank <%= index === 0 ? 'gold' : index === 1 ? 'silver' : 'bronze' %>">
                                    <%= index === 0 ? '🥇' : index === 1 ? '🥈' : '🥉' %>
                                </span>
                                <span class="username"><%= rank.username %></span>
                            </div>
                            <span class="score"><%= rank.total_score %>점</span>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">아직 랭킹 데이터가 없습니다</div>
                <% } %>
            </div>

            <div class="card">
                <h2>📈 일일 랭킹</h2>
                <% if (dailyRanking && dailyRanking.length > 0) { %>
                    <% dailyRanking.slice(0, 3).forEach((rank, index) => { %>
                        <div class="ranking-item">
                            <div class="rank-info">
                                <span class="rank <%= index === 0 ? 'gold' : index === 1 ? 'silver' : 'bronze' %>">
                                    <%= index === 0 ? '🥇' : index === 1 ? '🥈' : '🥉' %>
                                </span>
                                <span class="username"><%= rank.username %></span>
                            </div>
                            <span class="score"><%= rank.daily_score %>점</span>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">아직 랭킹 데이터가 없습니다</div>
                <% } %>
            </div>
        </div>

        <div class="card" style="margin-bottom: 24px;">
            <h2>✅ 오늘의 할 일</h2>
            <% if (todayTodos && todayTodos.length > 0) { %>
                <ul class="todo-preview">
                    <% todayTodos.forEach(todo => { %>
                        <li>
                            <%= todo.subject %> - <%= todo.activity_type %>
                            <% if (todo.completed) { %>
                                <span style="color: #52c41a; margin-left: 8px;">✓</span>
                            <% } %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <div class="empty-state">오늘 할 일이 없습니다</div>
            <% } %>
        </div>
    </div>
</body>
</html>